//# query = "INSERT INTO users"
// 		# query = query + " (first_name, last_name, email, password, created_at, updated_at)"
// 		# query = query + " VALUES('" + fname + "','" + lname + "','" + email + "','" + pw_hash + "', NOW(), NOW())"
// 		# user = mysql.query_db(query)
// # save mode
// 
							// <!-- NEED TO WORK ON THIS...
							// {% for i in range(0,length(comments))	%}
							// {% if comments[i]['id'] == post['id'] %}
							// {% cs = comments[i]['comments'] %}
							// {% for j in range(length(cs)) %}
							// <p>{{ cs[j]['u.fname'] }} + {{ cs[j]['u.lname'] }}</p>
							// <p>wrote at {{ cs[j]['created_at']}}</p>
							// <p>{{ cs[j]['comment'] }}</p>
							// {% endfor %}
							// {% endif %}
							// {% endfor %} -->
